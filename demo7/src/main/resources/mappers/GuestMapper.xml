<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org//mybatis-3-mapper.dtd">

<mapper namespace="com.wt.mapper.GuestMapper">
    <select id="getAllGuest" resultType="com.wt.bean.Guest">
        select id,name,role from guest;
    </select>
</mapper>